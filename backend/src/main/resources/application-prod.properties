# Production Application Configuration for Google Cloud Run (Supabase PostgreSQL)
spring.application.name=CodeReviewer
server.port=${PORT:8080}

# Supabase Cloud PostgreSQL
spring.datasource.url=jdbc:postgresql://aws-0-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.username=postgres.pwfmafhtlvbrogfjcocs
spring.datasource.password=#u2yU4MBNazVfB!
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Non-blocking in-memory or disabled components for serverless Cloud Run
spring.kafka.listener.auto-startup=false
spring.data.redis.repositories.enabled=false

# Groq AI
groq.api.key=${GROQ_API_KEY:gsk_mock_api_key_for_testing}
groq.api.url=https://api.groq.com/openai/v1/chat/completions
groq.model=openai/gpt-oss-120b
groq.fallback-model=llama-3.1-8b-instant

# JWT Secret
app.jwt.secret=${APP_JWT_SECRET:C3XhEpJMGFodmMirn0D29Ijp3xvZpobwip6hZQ69qSY=}

# CORS
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:*}
